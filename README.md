# 🍵 Tea House Blog

مدونة احترافية مبنية باستخدام **Hugo** وثيم **Blowfish**، تتميز بتصميم هادئ، دعم كامل للغتين (العربية والإنجليزية)، وتأثير مطر مخصص.

## 🚀 المميزات
- **تعدد اللغات:** دعم كامل للعربية (RTL) والإنجليزية (LTR).
- **التصميم:** تأثير مطر (Rain Effect) باستخدام CSS/JS وخلفية Hero كاملة الشاشة.
- **المقالات:** وقت القراءة، عدد المشاهدات، الوسوم، ومقالات ذات صلة.
- **التفاعل:** نظام تعليقات Giscus وزر "Buy Me a Coffee".
- **التقنية:** بحث سريع عبر Fuse.js، ونشر تلقائي عبر GitHub Actions.

## 🛠️ هيكلية المجلدات
- `content/ar`: المحتوى العربي.
- `content/en`: المحتوى الإنجليزي.
- `assets/css/custom.css`: تنسيقات المطر والخلفية.
- `assets/js/rain.js`: كود توليد المطر.
- `.github/workflows/hugo.yml`: إعدادات النشر التلقائي.

## 💻 التشغيل المحلي
1. تأكد من تثبيت Hugo Extended.
2. قم بتحميل المستودع:
   ```bash
   git clone --recursive <your-repo-url>
   cd tea-house
   ```
3. شغل الخادم المحلي:
   ```bash
   hugo server
   ```

## 📝 ملاحظات الإعداد
- قم بتغيير `baseURL` في ملف `hugo.toml` ليتناسب مع رابط موقعك.
- قم بتحديث معرفات Giscus و Buy Me a Coffee في الإعدادات.
