name: Update GitHub Profile README

on:
  push:
    branches:
      - main
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Blog Repo
        uses: actions/checkout@v3
      
      - name: Checkout Profile Repo
        uses: actions/checkout@v3
        with:
          repository: moon9io/moon9io
          token: ${{ secrets.PROFILE_UPDATE_TOKEN }}
          path: profile
      
      - name: Update README
        run: |
          # Extract latest 3 posts from index.json or public folder
          # This is a simplified version, in a real scenario we'd parse the RSS or JSON
          echo "Updating profile README with latest posts..."
          # Logic to update profile/README.md between markers
          
      - name: Commit and Push
        run: |
          cd profile
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add .
          git commit -m "Update latest blog posts" || exit 0
          git push
